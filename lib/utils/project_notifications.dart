import 'package:flutter/cupertino.dart';

class RecentEmojisSection extends ScrollUpdateNotification {
  RecentEmojisSection(
      {required BuildContext context, required ScrollMetrics metrics})
      : super(context: context, metrics: metrics);
  entered() {
    dispatch(context);
  }
}

class SmileysEmojisSection extends ScrollUpdateNotification {
  SmileysEmojisSection(
      {required BuildContext context, required ScrollMetrics metrics})
      : super(context: context, metrics: metrics);
  entered() {
    dispatch(context);
  }
}

class AnimalsEmojiSection extends ScrollUpdateNotification {
  AnimalsEmojiSection(
      {required BuildContext context, required ScrollMetrics metrics})
      : super(context: context, metrics: metrics);
  entered() {
    dispatch(context);
  }
}

class FoodEmojisSection extends ScrollUpdateNotification {
  FoodEmojisSection(
      {required BuildContext context, required ScrollMetrics metrics})
      : super(context: context, metrics: metrics);
  entered() {
    dispatch(context);
  }
}

class ActivityEmojisSection extends ScrollUpdateNotification {
  ActivityEmojisSection(
      {required BuildContext context, required ScrollMetrics metrics})
      : super(context: context, metrics: metrics);
  entered() {
    dispatch(context);
  }
}

class TravelEmojisSection extends ScrollUpdateNotification {
  TravelEmojisSection(
      {required BuildContext context, required ScrollMetrics metrics})
      : super(context: context, metrics: metrics);
  entered() {
    dispatch(context);
  }
}

class ObjectsEmojisSection extends ScrollUpdateNotification {
  ObjectsEmojisSection(
      {required BuildContext context, required ScrollMetrics metrics})
      : super(context: context, metrics: metrics);
  entered() {
    dispatch(context);
  }
}

class SymbolsEmojisSection extends ScrollUpdateNotification {
  SymbolsEmojisSection(
      {required BuildContext context, required ScrollMetrics metrics})
      : super(context: context, metrics: metrics);
  entered() {
    dispatch(context);
  }
}

class FlagsEmojisSection extends ScrollUpdateNotification {
  FlagsEmojisSection(
      {required BuildContext context, required ScrollMetrics metrics})
      : super(context: context, metrics: metrics);
  entered() {
    dispatch(context);
  }
}
